{{#if @site.members_enabled}}
    {{#unless @member}}
        <section class="gh-cta gh-outer">
            <div class="gh-cta-inner gh-inner">
                {{#if title}}
                    <h2 class="gh-cta-title">Kickstart your transformation</h2>
                {{/if}}
                <button class="gh-cta-input" data-portal>legend@mint.com <span>Start the change</span></button>
            </div>
        </section>
    {{/unless}}
{{/if}}